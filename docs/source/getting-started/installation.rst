Installation
============

Brackets is hosted on PyPI as a Python package, install a proper Python version,
then do a::

    pip install brackets

to get Brackets. Please note that currently CPython 3.5+ is officially
supported, it may work on other distributions too, but it is not tested.

Installing Brackets with Anaconda is recommended.
